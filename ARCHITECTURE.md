# Architecture & Implementation Details

## 🏢 System Architecture

### High-Level Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────────┐
│                            CLIENT LAYER                                  │
│                                                                           │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │            Next.js Frontend (React + TypeScript)                │   │
│  │                  http://localhost:3000                           │   │
│  │                                                                   │   │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │   │
│  │  │   Auth UI    │  │  Products UI │  │   Chat UI    │          │   │
│  │  │  (Login/Reg) │  │  (Dashboard) │  │  (Widget)    │          │   │
│  │  └──────────────┘  └──────────────┘  └──────────────┘          │   │
│  │         │                  │                  │                 │   │
│  │         └──────────────────┼──────────────────┘                 │   │
│  │                            │                                     │   │
│  │  ┌──────────────────────────────────────────────────────────┐  │   │
│  │  │     Custom Hooks (useAuth, useProducts, useChat)         │  │   │
│  │  └──────────────────────────────────────────────────────────┘  │   │
│  │                            │                                     │   │
│  │  ┌──────────────────────────────────────────────────────────┐  │   │
│  │  │  React Query (State Mgmt, Caching, Auto-sync)            │  │   │
│  │  └──────────────────────────────────────────────────────────┘  │   │
│  │                            │                                     │   │
│  │  ┌──────────────────────────────────────────────────────────┐  │   │
│  │  │  Axios HTTP Client (Token Management, Error Handling)    │  │   │
│  │  └──────────────────────────────────────────────────────────┘  │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                            │                                            │
└────────────────────────────┼────────────────────────────────────────────┘
                             │
                    HTTP Requests (REST)
                             │
┌────────────────────────────┼────────────────────────────────────────────┐
│                   API GATEWAY LAYER                                      │
│                                                                           │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │           Next.js API Routes (/api/*)                           │   │
│  │                                                                   │   │
│  │  POST /api/chat  ───> Calls OpenRouter ──> Returns AI Response  │   │
│  │                                                                   │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                            │                                            │
│                            └─────────────────┬────────────────────┐    │
│                                              │                    │    │
│                                     (Fetch products)      (Save history)│
│                                              │                    │    │
└──────────────────────────────────────────────┼────────────────────┼────┘
                                               │                    │
                         HTTP Requests         │                    │
                    (REST API calls)           │                    │
                                               │                    │
┌──────────────────────────────────────────────┼────────────────────┼────┐
│              BACKEND API LAYER                │                    │    │
│                                               │                    │    │
│  ┌────────────────────────────────────────────────────────────┐  │    │
│  │  Django REST Framework                                     │  │    │
│  │  http://localhost:8000/api                                 │  │    │
│  │                                                             │  │    │
│  │  ┌──────────────────┐  ┌──────────────────┐              │  │    │
│  │  │  Auth ViewSet    │  │  Product ViewSet │              │  │    │
│  │  │  ├─ Register     │  │  ├─ List         │              │  │    │
│  │  │  ├─ Login        │  │  ├─ Create       │              │  │    │
│  │  │  ├─ Refresh      │  │  ├─ Update       │              │  │    │
│  │  │  ├─ Get User     │  │  ├─ Delete       │              │  │    │
│  │  │  └─ List Users   │  │  ├─ Approve      │              │  │    │
│  │  └──────────────────┘  └──────────────────┘              │  │    │
│  │                                                             │  │    │
│  │  ┌──────────────────┐  ┌──────────────────┐              │  │    │
│  │  │  Chat ViewSet    │  │  Filters & Auth  │              │  │    │
│  │  │  ├─ Get History  │  │  ├─ Permissions  │              │  │    │
│  │  │  └─ Save Message │  │  └─ Multi-tenant │              │  │    │
│  │  └──────────────────┘  └──────────────────┘              │  │    │
│  │                                                             │  │    │
│  └────────────────────────────────────────────────────────────┘  │    │
│                            │                                      │    │
│                            │                                      │    │
│  ┌────────────────────────────────────────────────────────────┐  │    │
│  │  Django ORM (Models & Serializers)                         │  │    │
│  │                                                             │  │    │
│  │  ┌───────────┐  ┌─────────┐  ┌──────────┐ ┌──────────┐   │  │    │
│  │  │   User    │  │Business │  │ Product  │ │   Chat   │   │  │    │
│  │  │ - email   │  │- name   │  │- status  │ │ History  │   │  │    │
│  │  │ - role    │  │- users  │  │- price   │ │- messages│   │  │    │
│  │  │ - business│  │-products│  │-business │ │-history  │   │  │    │
│  │  └───────────┘  └─────────┘  └──────────┘ └──────────┘   │  │    │
│  │                                                             │  │    │
│  └────────────────────────────────────────────────────────────┘  │    │
│                            │                                      │    │
└────────────────────────────┼──────────────────────────────────────┘    │
                             │                                            │
                   SQL Queries                                            │
                             │                                            │
┌────────────────────────────┼──────────────────────────────────────┐   │
│                    DATA LAYER                                      │   │
│                                                                    │   │
│  ┌──────────────────────────────────────────────────────────┐   │   │
│  │           PostgreSQL 15 Database                          │   │   │
│  │         (Supabase AWS, EU Region)                         │   │   │
│  │                                                            │   │   │
│  │  Tables:                                                  │   │   │
│  │  ├─ auth_user (Custom User model)                         │   │   │
│  │  ├─ authentication_business                               │   │   │
│  │  ├─ authentication_role                                   │   │   │
│  │  ├─ products_product                                      │   │   │
│  │  └─ chat_chathistory                                      │   │   │
│  │                                                            │   │   │
│  │  Indexes: (business, status), (business, timestamp)       │   │   │
│  │  Relationships: Foreign keys, constraints                 │   │   │
│  │                                                            │   │   │
│  └──────────────────────────────────────────────────────────┘   │   │
│                                                                    │   │
└────────────────────────────────────────────────────────────────┘   │
```

---

## 🔐 Authentication & Authorization Flow

```
┌──────────────────────────────────────────────────────────────────┐
│                    USER AUTHENTICATION FLOW                        │
└──────────────────────────────────────────────────────────────────┘

1. REGISTRATION
   ┌─────────────┐
   │   Frontend  │
   │  Register   │ ─────────────────────────────┐
   │    Form     │                               │
   └─────────────┘                               │
                                                 ▼
                                    ┌──────────────────────┐
                                    │ POST /auth/register/ │
                                    │                      │
                                    │ Validates email,    │
                                    │ password strength,   │
                                    │ business uniqueness  │
                                    └──────────────────────┘
                                                 │
                                                 ▼
                                    ┌──────────────────────┐
                                    │  Hash password       │
                                    │  Create User record  │
                                    │  Assign to Business  │
                                    │  Set default role    │
                                    └──────────────────────┘
                                                 │
                                                 ▼
                    ┌────────────────────────────────────────┐
                    │  User account created & ready to login  │
                    └────────────────────────────────────────┘

2. LOGIN
   ┌─────────────┐
   │   Frontend  │
   │  Login Form │ ─────────────────────────────┐
   │ email+pwd   │                               │
   └─────────────┘                               │
                                                 ▼
                                    ┌──────────────────────┐
                                    │ POST /auth/login/    │
                                    │                      │
                                    │ TokenObtainPairView  │
                                    │ (SimpleJWT)          │
                                    └──────────────────────┘
                                                 │
                                    ┌────────────┴────────────┐
                                    ▼                         ▼
                           ┌──────────────┐        ┌──────────────┐
                           │ Verify email │        │ Verify pwd   │
                           │  exists      │        │  matches     │
                           └──────────────┘        └──────────────┘
                                    │                         │
                                    └────────────┬────────────┘
                                                 ▼
                                    ┌──────────────────────┐
                                    │  Generate JWT tokens │
                                    │                      │
                                    │  Access token:  1hr  │
                                    │  Refresh token: 24hr │
                                    └──────────────────────┘
                                                 │
                                                 ▼
                    ┌────────────────────────────────────────┐
                    │ Return: {access, refresh}              │
                    │ Frontend stores in localStorage        │
                    └────────────────────────────────────────┘

3. API REQUEST WITH AUTHENTICATION
   ┌──────────────┐
   │  GET /api/   │ ───────────┐
   │  products/   │            │
   └──────────────┘            │
        │                       │
        │ Attach header:        │
        │ Authorization:        │
        │ Bearer {access_token} │
        │                       │
        ├───────────────────────┘
        │
        ▼
   ┌────────────────────────────┐
   │  Django REST Framework     │
   │  Checks JWT signature      │
   │  Validates not expired     │
   │  Sets request.user         │
   └────────────────────────────┘
        │
        ├─ Valid ─────────────┐
        │                     ▼
        │              ┌──────────────┐
        │              │ Allow access │
        │              │ to resource  │
        │              └──────────────┘
        │
        └─ Expired ───────────┐
                              ▼
                     ┌──────────────────┐
                     │ Return 401 error │
                     │ (Unauthorized)   │
                     └──────────────────┘
                              │
                              ▼
                     ┌──────────────────┐
                     │ Frontend catches │
                     │ 401 response     │
                     └──────────────────┘

4. AUTO TOKEN REFRESH
   ┌──────────────────────────┐
   │ Access token expires in  │
   │ < 5 minutes              │
   └──────────────────────────┘
              │
              ▼
   ┌──────────────────────────────┐
   │ POST /auth/refresh/          │
   │ { refresh: refresh_token }   │
   └──────────────────────────────┘
              │
              ▼
   ┌──────────────────────────────┐
   │ Django validates refresh     │
   │ token signature & expiration │
   └──────────────────────────────┘
              │
              ├─ Valid ──────────┐
              │                  ▼
              │         ┌─────────────────┐
              │         │ Issue new       │
              │         │ access token    │
              │         │ (1 hour valid)  │
              │         └─────────────────┘
              │                  │
              │                  ▼
              │         ┌─────────────────┐
              │         │ Frontend stores │
              │         │ new token       │
              │         │ User unaware    │
              │         └─────────────────┘
              │
              └─ Expired ────────┐
                                 ▼
                        ┌─────────────────┐
                        │ Redirect to     │
                        │ /login          │
                        │ Clear tokens    │
                        └─────────────────┘
```

---

## 👥 Role-Based Access Control (RBAC) Implementation

```
┌─────────────────────────────────────────────────────────────────┐
│                     PERMISSION MODEL                             │
└─────────────────────────────────────────────────────────────────┘

Database Structure:
┌──────────────┐
│ Role Model   │
├──────────────┤
│ id           │
│ name ────┐   │  Choices: admin, editor, approver, viewer
│ permissions│   │  (JSON field - flexible)
└──────────────┘

User Model:
┌──────────────┐
│ User         │
├──────────────┤
│ id           │
│ role (FK)  ──┼──> Role
│ business   ──┼──> Business (multi-tenant)
└──────────────┘

Permission Checking:

For View Access:
  ┌─────────────────────────────────┐
  │ Check: user.role.name in action │
  │ permission list                  │
  └─────────────────────────────────┘
           │
      ├────┼────┬─────┬──────┐
      │    │    │     │      │
    Admin Editor Approv Viewer
      │    │     │     │
      ✅   ✅    ⚠️    ❌  (Create Product)
      ✅   ✅    ⚠️    ❌  (Edit Product)
      ✅   ❌    ❌    ❌  (Delete Product)
      ✅   ❌    ✅    ❌  (Approve Product)
      ✅   ❌    ❌    ❌  (Manage Users)

Multi-Tenancy Filter:
Every query filters by: business=request.user.business

Example Product Query:
  Product.objects.filter(
      business=request.user.business,
      status='approved'  # or based on role
  )

Result: Users only see their business's data
```

---

## 📦 Product Workflow State Machine

```
┌───────────────────────────────────────────────────────────────┐
│              PRODUCT LIFECYCLE                                 │
└───────────────────────────────────────────────────────────────┘

CREATION:
┌─────────────────────────────────┐
│  Editor creates product via form │
└────────────┬────────────────────┘
             │
             ▼
┌─────────────────────────────────┐
│  Frontend POST /api/products/   │
└────────────┬────────────────────┘
             │
             ▼
┌─────────────────────────────────┐
│  Backend validates with          │
│  ProductSerializer               │
│  - All required fields           │
│  - Price >= 0                    │
│  - Description not empty         │
└────────────┬────────────────────┘
             │
             ▼
┌─────────────────────────────────┐
│  perform_create() sets:          │
│  - status = 'draft'              │
│  - created_by = request.user     │
│  - business = user.business      │
│  - created_at = now()            │
└────────────┬────────────────────┘
             │
             ▼
    ┌────────────────────┐
    │  Saved to DB       │
    │  (Private to user) │
    └────────────────────┘

SUBMISSION FOR APPROVAL:
┌─────────────────────────────────┐
│  Editor clicks "Submit for       │
│  Approval" button                │
└────────────┬────────────────────┘
             │
             ▼
┌─────────────────────────────────┐
│  Frontend PATCH /products/{id}/  │
│  { "status": "pending_approval" }│
└────────────┬────────────────────┘
             │
             ▼
┌─────────────────────────────────┐
│  ProductViewSet.partial_update() │
│  - Validates user permission     │
│  - Checks ownership              │
└────────────┬────────────────────┘
             │
             ▼
    ┌────────────────────┐
    │  Status changed    │
    │  to                │
    │  'pending_approval'│
    └────────────────────┘

APPROVAL:
┌─────────────────────────────────┐
│  Approver reviews product       │
│  Clicks "Approve" button        │
└────────────┬────────────────────┘
             │
             ▼
┌─────────────────────────────────┐
│  Frontend POST /products/{id}/   │
│  approve/                        │
└────────────┬────────────────────┘
             │
             ▼
┌─────────────────────────────────┐
│  Backend:                        │
│  - Checks if user is Approver   │
│  - Checks status is 'pending'   │
└────────────┬────────────────────┘
             │
             ▼
┌─────────────────────────────────┐
│  Calls product.approve(user)    │
│  Sets:                           │
│  - status = 'approved'           │
│  - approved_by = approver        │
│  - approved_at = now()           │
└────────────┬────────────────────┘
             │
             ▼
    ┌────────────────────┐
    │  Product is now    │
    │  VISIBLE TO PUBLIC │
    │  GET /products/    │
    └────────────────────┘

VISIBILITY RULES:

Status: Draft
├─ Who can see: Owner user, users in same business
└─ Visible in: /api/products/list_internal/ (with filter)

Status: Pending Approval
├─ Who can see: Owner user, Approvers, Admins
└─ Visible in: /api/products/list_internal/ (with filter)

Status: Approved
├─ Who can see: Everyone (public)
└─ Visible in: /api/products/ (public endpoint)

Status: Rejected
├─ Who can see: Owner user, Admins
└─ Visible in: /api/products/list_internal/ (with filter)
```

---

## 💬 Chat Integration Flow

```
┌─────────────────────────────────────────────────────────────┐
│                  CHAT MESSAGE FLOW                           │
└─────────────────────────────────────────────────────────────┘

1. USER SENDS MESSAGE
   ┌──────────────────┐
   │  ChatWidget      │
   │  Input: "Show me │
   │  computers"      │
   └────────┬─────────┘
            │
            ▼
   ┌──────────────────────────────────────────┐
   │ handleSend()                             │
   │ ├─ Validate: message not empty          │
   │ ├─ Disable input (loading = true)       │
   │ └─ Call sendMessage(msg, businessId)    │
   └────────┬─────────────────────────────────┘
            │
            ▼
   ┌──────────────────────────────────────────┐
   │ Frontend Fetch:                          │
   │ POST /api/chat                           │
   │ {                                        │
   │   message: "Show me computers",          │
   │   businessId: 4                          │
   │ }                                        │
   └────────┬─────────────────────────────────┘
            │
            ▼
2. NEXT.JS API ROUTE HANDLES
   ┌──────────────────────────────────────────┐
   │ frontend/app/api/chat/route.ts           │
   │                                          │
   │ 1. Extract message & businessId         │
   │ 2. Log for debugging                    │
   │ 3. Check OPENROUTER_API_KEY exists      │
   └────────┬─────────────────────────────────┘
            │
            ▼
3. FETCH BUSINESS PRODUCTS FOR CONTEXT
   ┌──────────────────────────────────────────┐
   │ fetch('/api/products/list_internal/',     │
   │   params: { business: 4 }                │
   │ )                                        │
   │                                          │
   │ Returns products in JSON format:        │
   │ [                                        │
   │   {                                      │
   │     id: 29, name: "Computer",            │
   │     description: "...",                  │
   │     price: "999.99", status: "approved"  │
   │   },                                     │
   │   ...                                    │
   │ ]                                        │
   └────────┬─────────────────────────────────┘
            │
            ▼
4. CONSTRUCT AI PROMPT
   ┌──────────────────────────────────────────┐
   │ System Message:                          │
   │ "You are a helpful product assistant     │
   │  for a marketplace business. You have    │
   │  access to the following products..."    │
   │                                          │
   │ Context:                                 │
   │ [ALL PRODUCTS IN JSON]                   │
   │                                          │
   │ User Message:                            │
   │ "Show me computers"                      │
   └────────┬─────────────────────────────────┘
            │
            ▼
5. CALL OPENROUTER API
   ┌──────────────────────────────────────────┐
   │ openai.chat.completions.create({         │
   │   model: "mistralai/mistral-7b-instruct" │
   │   messages: [                            │
   │     { role: "system", content: context } │
   │     { role: "user", content: message }   │
   │   ],                                     │
   │   temperature: 0.7,                      │
   │   max_tokens: 500                        │
   │ })                                       │
   └────────┬─────────────────────────────────┘
            │
            ▼
   ┌──────────────────────────────────────────┐
   │ Mistral 7B Model:                        │
   │ ├─ Analyzes user message                │
   │ ├─ Searches product JSON context        │
   │ ├─ Generates relevant response          │
   │ └─ Returns: "We have computers...       │
   │              Priced at $999.99..."       │
   └────────┬─────────────────────────────────┘
            │
            ▼
6. SAVE TO CHAT HISTORY
   ┌──────────────────────────────────────────┐
   │ fetch('/api/chat/history/', {            │
   │   method: 'POST',                        │
   │   body: {                                │
   │     user_message: "Show me computers",   │
   │     ai_response: "We have computers..." │
   │   }                                      │
   │ })                                       │
   │                                          │
   │ Backend ChatHistorySerializer:           │
   │ ├─ Sets user = request.user              │
   │ ├─ Sets business = user.business         │
   │ ├─ Sets timestamp = now()               │
   │ └─ Saves to DB                          │
   └────────┬─────────────────────────────────┘
            │
            ▼
7. RETURN RESPONSE TO FRONTEND
   ┌──────────────────────────────────────────┐
   │ { response: "We have computers..." }     │
   │                                          │
   │ Frontend receives & displays in          │
   │ ChatWidget                               │
   └────────┬─────────────────────────────────┘
            │
            ▼
   ┌──────────────────────────────────────────┐
   │ MessageBubble displays AI response       │
   │ ChatWidget re-enables input              │
   │ User can continue conversation           │
   └──────────────────────────────────────────┘

ERROR HANDLING:

If OpenRouter fails:
  → Return error message to frontend
  → User sees: "Sorry, I encountered an error"
  → Console logs error details for debugging

If product fetch fails:
  → Continue with empty product context
  → AI gives generic product info response
  → Still saves to history if possible

If history save fails:
  → Response already sent to user
  → Error logged, doesn't affect UX
```

---

## 🗂️ Request/Response Data Flow Examples

### Create Product
```
REQUEST:
POST /api/products/
Authorization: Bearer eyJhbGciOiJIUzI1NiIs...
{
  "name": "Laptop",
  "description": "High performance laptop",
  "price": "1299.99",
  "status": "pending_approval"
}

RESPONSE (201 Created):
{
  "id": 42,
  "name": "Laptop",
  "description": "High performance laptop",
  "price": "1299.99",
  "status": "pending_approval",
  "business": 4,
  "business_name": "MIc Business",
  "created_by": 15,
  "created_by_name": "miceditor",
  "approved_by": null,
  "created_at": "2026-02-19T10:30:00Z",
  "updated_at": "2026-02-19T10:30:00Z",
  "approved_at": null
}
```

### List Products (Paginated)
```
REQUEST:
GET /api/products/?page=1

RESPONSE (200 OK):
{
  "count": 42,
  "next": "http://localhost:8000/api/products/?page=2",
  "previous": null,
  "results": [
    {
      "id": 29,
      "name": "Fast Food",
      "price": "43.99",
      "status": "approved",
      ...
    },
    ...
  ]
}
```

### Get Chat History
```
REQUEST:
GET /api/chat/history/?page=1

RESPONSE (200 OK):
{
  "count": 15,
  "next": "http://localhost:8000/api/chat/history/?page=2",
  "previous": null,
  "results": [
    {
      "id": 5,
      "user_message": "Show me products under $100",
      "ai_response": "Based on your inventory...",
      "timestamp": "2026-02-19T09:45:00Z"
    },
    ...
  ]
}
```

---

## 🔄 Data Update Patterns

### Real-time Updates with React Query
```javascript
// hooks/useProducts.ts
const fetchProducts = useCallback(async (params: any = {}) => {
  const response = await apiClient.get('/products/', { params });
  // React Query automatically:
  // 1. Caches the response
  // 2. Marks stale after 5 minutes
  // 3. Refetches in background
  // 4. Deduplicates identical requests
  // 5. Notifies components of updates
}, []);

// In component:
const { data, isLoading, error } = useQuery({
  queryKey: ['products'],
  queryFn: () => fetchProducts()
});
```

---

## 🔌 API Client Interceptor Pattern

```typescript
// Axios Request Interceptor
apiClient.interceptors.request.use((config) => {
  // 1. Log the request
  console.log(`📡 API Request: ${config.method} ${config.url}`);
  
  // 2. Attach token if available
  if (typeof window !== 'undefined') {
    const token = localStorage.getItem('access_token');
    if (token) {
      config.headers.Authorization = `Bearer ${token}`;
    }
  }
  
  return config;
});

// Axios Response Interceptor
apiClient.interceptors.response.use(
  (response) => {
    // Success: log and return
    console.log(`✅ API Success: ${response.config.method} ${response.config.url}`);
    return response;
  },
  async (error) => {
    // 401 Unauthorized
    if (error.response?.status === 401 && !originalRequest._retry) {
      originalRequest._retry = true;
      
      // Get refresh token
      const refreshToken = localStorage.getItem('refresh_token');
      
      // Call refresh endpoint
      const response = await axios.post('/auth/refresh/', {
        refresh: refreshToken
      });
      
      // Save new token
      localStorage.setItem('access_token', response.data.access);
      
      // Retry original request
      return apiClient(originalRequest);
    }
    
    return Promise.reject(error);
  }
);
```

This architecture ensures:
- Automatic token refresh without user intervention
- Consistent error handling across all API calls
- Request/response logging for debugging
- Type-safe API interactions

---

**Architecture Version**: 1.0  
**Last Updated**: February 19, 2026  
**Status**: Production-Ready (with proper environment setup)
